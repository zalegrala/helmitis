# stamped chart output (examples write here in docs/CI)
/_chart/
/chart/
/tmp-chart/

# built binaries — build with: go build -o bin/stamp ./cmd/stamp
# (avoid a bare /stamp entry: it would also match the stamp/ package directory)
/bin/
